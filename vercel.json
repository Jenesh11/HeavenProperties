{
  "builds": [
    {
      "src": "sanity/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "sanity/dist"
      }
    }
  ],
  "routes": [
    { "src": "/studio/(.*)", "dest": "sanity/dist/$1" }
  ]
}
